# Neon Postgres (Vercel Marketplace integration injects these automatically)
# Pooled connection, used by the app at runtime
DATABASE_URL=
# Direct connection, used by drizzle-kit (schema push) and the seed script
DATABASE_URL_UNPOOLED=

# Public URL of the site (canonical, sitemap, robots)
NUXT_PUBLIC_SITE_URL=
